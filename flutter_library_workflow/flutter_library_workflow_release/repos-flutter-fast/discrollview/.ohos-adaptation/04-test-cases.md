# Discrollview 黑盒测试用例

**生成日期**: 2026-07-30
**项目名称**: discrollview 鸿蒙化适配
**测试套件**: discrollview-test-suite
**测试用例总数**: 30

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 插件名称 | discrollview |
| 版本 | 0.0.2 |
| 测试范围 | 8 个功能模块，30 个测试用例 |
| 测试设备 | phone / tablet / 2in1 |
| 测试类型 | 黑盒功能测试 |

## 测试用例级别分布

| 级别 | 数量 | 占比 |
|------|------|------|
| L0 | 13 | 43.3% |
| L1 | 11 | 36.7% |
| L2 | 6 | 20.0% |
| **合计** | **30** | **100%** |

---

## 模块一览

| 模块编号 | 模块名称 | 优先级 | 用例数 |
|----------|----------|--------|--------|
| F-01 | DiscrollveWidget 根滚动容器 | P0 | 4 |
| F-02 | DiscrollveContent 内容布局 | P0 | 2 |
| F-03 | DiscrollveConfig 变换配置 | P0 | 6 |
| F-04 | DiscrollveDirection 方向枚举 | P1 | 7 |
| F-05 | 滚动比例计算引擎 | P0 | 4 |
| F-06 | 变换渲染器 | P0 | 2 |
| F-07 | 阈值控制 | P1 | 4 |
| F-08 | 重置/恢复 | P0 | 2 |

---

## F-01 DiscrollveWidget 根滚动容器 (P0)

### F-01-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-01-01 | 验证 DiscrollveWidget 正常构建并显示子 Widget | L0 | Flutter 应用已启动，进入 Discrollview Demo 页面 | 1. 观察页面顶部区域 → 显示深蓝色渐变背景的静态头部，包含 'Discrollview Demo' 标题文字和滚动提示；2. 观察头部下方 → 显示至少一个可滚动的子卡片（如 'Alpha + fromBottom' 卡片） | 页面正常渲染，HTML 头部和至少一个子卡片均可见，无异常或白屏 | 页面保持在初始滚动位置（顶部） |

### F-01-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-01-02 | 验证静态头部占据全屏高度 | L0 | 在 Discrollview Demo 页面，位于顶部滚动位置 | 1. 观察静态头部区域的高度 → 头部从屏幕顶部延伸至屏幕底部，占据完整的可视区高度；2. 检查头部下方是否有其他内容可见 → 第一个动画卡片完全不可见，隐藏在屏幕底部以下 | 静态头部占据整个 viewport 高度，后续子卡片需要向下滚动才能看到 | 无 |

### F-01-03
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-01-03 | 验证外部 ScrollController 可正常控制滚动 | L1 | Discrollview Demo 页面已加载 | 1. 通过程序化方式获取 ScrollController → ScrollController 实例可用且 hasClients 为 true；2. 通过 ScrollController 执行 animateTo 滚动到指定位置 → 页面滚动到目标位置，动画子 Widget 的变换状态随之更新 | ScrollController 正常工作，可程序化控制滚动位置并触发相应的变换效果 | 页面停留在程序化滚动后的位置 |

### F-01-04
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-01-04 | 验证少于 2 个子 Widget 时构造失败 | L2 | 编写测试代码尝试创建只有 1 个 DiscrollveContent 子 Widget | 1. 执行测试代码构建 DiscrollveWidget(children: [single]) → 构造阶段触发断言错误 | AssertionError 抛出，错误信息包含 'at least 2 children' | 正常处理异常，不影响应用其他页面 |

---

## F-02 DiscrollveContent 内容布局 (P0)

### F-02-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-02-01 | 验证 DiscrollveContent.child 正确关联 config | L0 | 编写测试验证工厂方法 | 1. 调用 DiscrollveContent.child(config: DiscrollveConfig(alpha: true), child: Text('Test')) → 返回的 Widget 中 config.alpha 为 true | DiscrollveContent 正确持有 config 和 child 的引用 | 无 |

### F-02-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-02-02 | 验证 config.none 的子 Widget 无任何变换 | L0 | Demo 页面包含 config.none 的子 Widget（底部 'Scroll back up to reset'） | 1. 滚动页面到底部 → 该 Widget 始终完全可见，无透明度变化；2. 回滚到顶部 → 该 Widget 持续可见，无缩放/平移/颜色变化 | config.none 的子 Widget 在滚动全程保持原始外观 | 无 |

---

## F-03 DiscrollveConfig 变换配置 (P0)

### F-03-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-01 | 验证 alpha 透明度变换 | L0 | Demo 页面存在 alpha: true 的卡片 | 1. 观察未触发卡片 → 卡片完全不可见；2. 向下滚动 → 卡片逐渐变为不透明；3. 完全触发后 → 卡片完全可见 | 透明度从 0.0 平滑过渡至 1.0 | 卡片保持 opacity=1.0 |

### F-03-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-02 | 验证 scaleX 水平缩放 | L0 | Demo 页面存在 scaleX: true 的卡片 | 1. 未触发时 → 卡片水平宽度为 0；2. 向下滚动 → 卡片水平宽度逐渐展开至完整宽度 | 卡片宽度平滑展开 | 卡片保持 scaleX=1.0 |

### F-03-03
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-03 | 验证 scaleY 垂直缩放 | L1 | Demo 页面存在 scaleX+scaleY 的卡片 | 1. 未触发时 → 卡片不可见；2. 向下滚动 → 卡片同时水平和垂直展开 | 卡片从 0×0 等比例放大 | 卡片保持 scaleX=1.0, scaleY=1.0 |

### F-03-04
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-04 | 验证 scaleX 和 scaleY 等比例缩放 | L1 | Demo 页面存在 scaleX+scaleY 的卡片 | 1. 观察缩放过程 → 卡片保持原始宽高比 | 卡片等比例缩放，无变形 | scaleX=1.0, scaleY=1.0 |

### F-03-05
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-05 | 验证背景色渐变 | L1 | Demo 页面存在 fromColor+toColor 的卡片 | 1. 未触发时 → 背景色为 fromColor（绿）；2. 滚动 → 颜色平滑过渡；3. 完全触发 → 背景色为 toColor（黑） | 背景色从起始色平滑过渡到目标色 | 背景色保持为 toColor |

### F-03-06
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-03-06 | 验证 DiscrollveConfig 默认值 | L2 | 编写单元测试 | 1. 创建 const DiscrollveConfig() → 所有属性为 false/-1/0.0；2. hasTransforms → 返回 false | 默认全关闭 | 无 |

---

## F-04 DiscrollveDirection 方向枚举 (P1)

### F-04-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-01 | 验证 fromBottom 方向平移 | L0 | Demo 页面存在 fromBottom 卡片 | 1. 未触发时 → 卡片在下方偏移位置；2. 滚动 → 卡片向上移入原位 | 从下方平滑移入 | 卡片在原位 |

### F-04-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-02 | 验证 fromTop 方向平移 | L1 | Demo 页面存在 fromTop 卡片 | 1. 未触发时 → 卡片在上方偏移位置；2. 滚动 → 卡片向下移入原位 | 从上方平滑移入 | 卡片在原位 |

### F-04-03
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-03 | 验证 fromLeft 方向平移 | L1 | Demo 页面存在 fromLeft 卡片 | 1. 未触发时 → 卡片在左方偏移位置；2. 滚动 → 卡片向右移入原位 | 从左方平滑移入 | 卡片在原位 |

### F-04-04
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-04 | 验证 fromRight 方向平移 | L1 | Demo 页面存在 fromRight 卡片 | 1. 未触发时 → 卡片在右方偏移位置；2. 滚动 → 卡片向左移入原位 | 从右方平滑移入 | 卡片在原位 |

### F-04-05
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-05 | 验证 fromBottom\|fromLeft 组合方向 | L0 | Demo 页面存在组合方向卡片 | 1. 未触发时 → 卡片在对角线偏移位置；2. 滚动 → 卡片沿对角线移入原位 | 对角线方向平滑移入 | 卡片在原位 |

### F-04-06
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-06 | 验证 fromTop+fromBottom 被拒绝 | L2 | 编写测试代码 | 1. 创建 fromTop\|fromBottom → 触发 AssertionError | 构造失败 | 应用不渲染错误状态 |

### F-04-07
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-04-07 | 验证 fromLeft+fromRight 被拒绝 | L2 | 编写测试代码 | 1. 创建 fromLeft\|fromRight → 触发 AssertionError | 构造失败 | 应用不渲染错误状态 |

---

## F-05 滚动比例计算引擎 (P0)

### F-05-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-05-01 | 验证中心触发模式 | L0 | 滚动内容足够长 | 1. 向下滚动 → 卡片中心接近屏幕中心时开始变换；2. 继续滚动 → 卡片顶部到达屏幕顶部时完全触发 | 中心对齐视口中心触发变换 | 卡片保持完全触发态 |

### F-05-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-05-02 | 验证顶部触发模式 | L1 | 底部卡片剩余空间不足 | 1. 滚动到底部 → 最后卡片触发时机早于前面卡片 | 空间不足时自动切换顶部触发 | 卡片完全触发 |

### F-05-03
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-05-03 | 验证 ratio 在 [0.0, 1.0] 范围内 | L0 | 单元测试 | 1. 输入 -0.5 → 返回 0.0；2. 输入 0.5 → 返回 0.5；3. 输入 1.5 → 返回 1.0 | ratio 始终在合法区间内 | 无 |

### F-05-04
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-05-04 | 验证零高度不触发 | L2 | 单元测试 | 1. childHeight=0 → calculateRatio 返回 null | 不触发变换 | 无 |

---

## F-06 变换渲染器 (P0)

### F-06-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-06-01 | 验证全组合变换同时生效 | L0 | Demo 页面存在全组合卡片 | 1. 未触发时 → 不可见+缩小+偏移；2. 滚动 → 渐变+放大+移入同时发生；3. 完全触发 → 可见+完整+原位 | 四种变换协调同步 | 卡片处于终态 |

### F-06-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-06-02 | 验证完全触发后所有变换达终态 | L1 | 任意变换卡片 ratio=1.0 | 1. 检查 alpha 卡片 → opacity=1.0；2. 检查 scale 卡片 → scale=1.0；3. 检查 translation 卡片 → translate=0 | 所有变换达终态 | 保持终态至回滚 |

---

## F-07 阈值控制 (P1)

### F-07-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-07-01 | 验证 threshold=0 立即触发 | L0 | Demo 页面 threshold 未设置的卡片 | 1. 缓慢滚动 → 刚进入触发区域立即开始变换 | 无延迟 | 无 |

### F-07-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-07-02 | 验证 threshold>0 延迟触发 | L0 | Demo 页面 threshold=0.2/0.4 的卡片 | 1. 进入触发区域前 20%/40% → 无变换；2. 超过阈值 → 变换开始并加速完成 | 阈值延迟生效，终态不受影响 | 卡片终态正常 |

### F-07-03
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-07-03 | 验证 withThreshold 终态一致性 | L1 | 单元测试 | 1. withThreshold(1.0, 0.0) → 1.0；2. withThreshold(1.0, 0.5) → 1.0；3. withThreshold(1.0, 0.9) → 1.0 | 终态不受阈值影响 | 无 |

### F-07-04
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-07-04 | 验证 threshold 越界被拒绝 | L2 | 单元测试 | 1. const DiscrollveConfig(threshold: -0.1) → 编译时断言失败 | AssertionError | 应用不使用无效值 |

---

## F-08 重置/恢复 (P0)

### F-08-01
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-08-01 | 验证回滚反向播放变换 | L0 | 卡片已完全触发 | 1. 缓慢向上回滚 → 变换效果反向播放；2. 继续回滚至离开触发区 → 卡片恢复初始状态 | 回滚平滑恢复初始状态 | 卡片回到初始隐藏状态 |

### F-08-02
| Case ID | 标题 | 级别 | 前置条件 | 测试步骤 | 预期结果 | 后置条件 |
|---------|------|------|----------|----------|----------|----------|
| F-08-02 | 验证 config.none 回滚无异常 | L1 | 底部存在 config.none 文本 | 1. 滚到底部再回滚到顶部 → 全程无闪烁/跳动/透明度变化 | config.none 不受滚动影响 | 无 |

---

## 覆盖统计

| 模块 | API | 用例数 | L0 | L1 | L2 |
|------|-----|--------|----|----|-----|
| F-01 DiscrollveWidget | DiscrollveWidget | 4 | 2 | 1 | 1 |
| F-02 DiscrollveContent | DiscrollveContent.child() | 2 | 2 | 0 | 0 |
| F-03 DiscrollveConfig | DiscrollveConfig | 6 | 2 | 3 | 1 |
| F-04 DiscrollveDirection | DiscrollveDirection | 7 | 2 | 3 | 2 |
| F-05 比例计算 | calculateRatio | 4 | 2 | 1 | 1 |
| F-06 变换渲染 | _applyTransforms | 2 | 1 | 1 | 0 |
| F-07 阈值控制 | withThreshold | 4 | 2 | 1 | 1 |
| F-08 重置恢复 | _resetTransforms | 2 | 1 | 1 | 0 |
| **合计** | | **30** | **13** | **11** | **6** |

---

*本测试用例由 flutter-fast 工作流生成 · 2026-07-30*
