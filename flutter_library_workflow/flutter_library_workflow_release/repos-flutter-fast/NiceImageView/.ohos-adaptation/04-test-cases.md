# NiceImageView 鸿蒙适配测试用例

本文件为 NiceImageView 鸿蒙适配测试套件（套件 ID：nice_image_view-ohos，应用包名：nice_image_view，应用卡片：nice_image_view demo）的完整测试用例说明，共 24 个用例，覆盖 8 个功能模块，所有用例均覆盖设备：phone,tablet,2in1。

## F-01 构造函数与初始化

### F-01-01 默认参数构建 [L0]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 使用默认参数创建 NiceImageView 组件，不传入任何参数 — 验证点：组件构建成功，无异常抛出
  2. 观察组件默认展示效果 — 验证点：图片按原始属性显示，无边框、无圆角、无遮罩
- **预期结果**：组件正常构建，默认属性与 Android 默认值一致
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1

### F-01-02 全参数构建 [L0]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 传入全部16个构造参数创建 NiceImageView 组件 — 验证点：组件构建成功，无异常抛出
  2. 逐一核对各参数对应的展示效果 — 验证点：全部参数被正确应用
- **预期结果**：组件构建成功且全部参数正确生效
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1

### F-01-03 自定义尺寸 [L0]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 指定 width 为120、height 为120 创建 NiceImageView 组件 — 验证点：组件占用区域与设定的宽高一致
  2. 修改宽高数值并重新构建组件 — 验证点：组件尺寸随 width 和 height 参数同步变化
- **预期结果**：组件尺寸与设定的 width 和 height 一致
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1

## F-02 圆形展示模式

### F-02-01 圆形模式渲染 [L1]
- **前置条件**：进入示例页面，打开“圆形模式”开关
- **测试步骤**：
  1. 确认“圆形模式”开关已开启 — 验证点：组件区域图片按圆形裁剪显示
  2. 观察图片边缘与四角区域 — 验证点：图片以圆形区域显示且无矩形溢出
- **预期结果**：图片按圆形区域裁剪渲染，无矩形溢出
- **后置条件**：关闭圆形模式，恢复为矩形显示
- **覆盖设备**：phone,tablet,2in1

### F-02-02 圆形模式不崩溃 [L0]
- **前置条件**：进入示例页面，打开“圆形模式”开关
- **测试步骤**：
  1. 在圆形模式下设置边框宽度为4并开启半透明遮罩 — 验证点：组件正常渲染，无异常抛出
  2. 反复切换圆形模式与矩形模式 — 验证点：组件不崩溃，渲染稳定
- **预期结果**：圆形模式结合边框与遮罩参数渲染不崩溃
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1

## F-03 圆角半径控制

### F-03-01 统一圆角渲染 [L1]
- **前置条件**：进入示例页面，关闭“圆形模式”切换为矩形模式
- **测试步骤**：
  1. 在矩形模式下将统一圆角参数设置为32 — 验证点：图片四角呈现统一圆角
  2. 观察四个角的圆角半径 — 验证点：四角圆角半径与 cornerRadius 参数一致
- **预期结果**：四角应用统一的圆角半径
- **后置条件**：将统一圆角参数复位为0
- **覆盖设备**：phone,tablet,2in1

### F-03-02 独立圆角渲染 [L1]
- **前置条件**：进入示例页面，关闭“圆形模式”切换为矩形模式
- **测试步骤**：
  1. 分别设置左上、右上、右下、左下四个独立圆角参数 — 验证点：各角圆角半径独立生效
  2. 修改其中某一个角的圆角半径 — 验证点：仅对应角发生变化，其余角保持不变
- **预期结果**：每个角的圆角半径与对应参数一致
- **后置条件**：将全部圆角参数复位为0
- **覆盖设备**：phone,tablet,2in1

### F-03-03 圆角加边框 [L1]
- **前置条件**：进入示例页面，关闭“圆形模式”切换为矩形模式
- **测试步骤**：
  1. 在矩形模式下设置统一圆角为32并设置边框宽度为4 — 验证点：边框沿圆角路径完整绘制
  2. 观察圆角与边框的衔接处 — 验证点：边框平滑贴合圆角路径，无直角突刺
- **预期结果**：边框沿圆角路径完整绘制
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1

## F-04 边框绘制

### F-04-01 外边框绘制 [L1]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 设置边框宽度为3并选择边框颜色为红色 — 验证点：图片外围出现红色外边框，宽度为3
  2. 修改边框颜色 — 验证点：外边框颜色随之更新，与设定一致
- **预期结果**：外边框宽度和颜色与设定一致
- **后置条件**：将边框宽度复位为0
- **覆盖设备**：phone,tablet,2in1

### F-04-02 内边框绘制 [L1]
- **前置条件**：进入示例页面，打开“圆形模式”开关
- **测试步骤**：
  1. 在圆形模式下设置内边框宽度为2、内边框颜色为白色 — 验证点：内边框在圆形区域边缘绘制且颜色正确
  2. 调整内边框宽度 — 验证点：内边框宽度随之更新
- **预期结果**：内边框在圆形区域边缘绘制且颜色正确
- **后置条件**：将内边框宽度复位为0
- **覆盖设备**：phone,tablet,2in1

### F-04-03 矩形模式忽略内边框 [L2]
- **前置条件**：进入示例页面，关闭“圆形模式”切换为矩形模式
- **测试步骤**：
  1. 在矩形模式下设置内边框宽度为10 — 验证点：组件区域不绘制内边框
  2. 观察图片边缘 — 验证点：无内边框出现，仅外边框（如有）生效
- **预期结果**：矩形模式下内边框参数被忽略且不接受绘制，不绘制内边框，符合预期
- **后置条件**：将内边框宽度复位为0
- **覆盖设备**：phone,tablet,2in1

## F-05 边框覆盖控制

### F-05-01 边框覆盖图片 [L1]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 打开“边框覆盖图片”开关并设置边框宽度为6 — 验证点：边框绘制在图片之上
  2. 观察图片与边框的重叠关系 — 验证点：图片被缩放适配，边框覆盖图片内容区域
- **预期结果**：边框绘制在图片之上且图片被缩放适配
- **后置条件**：关闭“边框覆盖图片”开关并复位边框宽度
- **覆盖设备**：phone,tablet,2in1

## F-06 遮罩绘制

### F-06-01 遮罩叠加绘制 [L1]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 开启半透明遮罩并使用非透明遮罩颜色 — 验证点：裁剪区域上叠加了遮罩颜色
  2. 观察图片明暗变化 — 验证点：遮罩颜色正确叠加且透明度符合预期
- **预期结果**：遮罩正确叠加在裁剪区域，透明度符合预期
- **后置条件**：关闭半透明遮罩
- **覆盖设备**：phone,tablet,2in1

## F-07 重绘逻辑

### F-07-01 图片变更触发重绘 [L0]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 将 image 属性更换为另一张图片 — 验证点：shouldRepaint 返回 true，画面更新为新图片
  2. 观察组件画面 — 验证点：新图片完整显示
- **预期结果**：修改 image 属性后触发重绘，画面更新
- **后置条件**：恢复原始图片
- **覆盖设备**：phone,tablet,2in1

### F-07-02 无变更不触发重绘 [L0]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 保持所有属性不变并刷新页面 — 验证点：shouldRepaint 返回 false，无重绘发生
  2. 对比重绘前后的画面 — 验证点：画面无任何变化
- **预期结果**：属性不变时 shouldRepaint 返回 false，不触发重绘
- **后置条件**：保持组件当前状态
- **覆盖设备**：phone,tablet,2in1

### F-07-03 圆形属性变更重绘 [L0]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 将“圆形模式”开关从关闭切换为开启 — 验证点：shouldRepaint 返回 true
  2. 观察画面由矩形切换为圆形 — 验证点：圆形与矩形切换生效
- **预期结果**：切换 isCircle 属性触发重绘，圆形与矩形切换生效
- **后置条件**：关闭“圆形模式”开关
- **覆盖设备**：phone,tablet,2in1

### F-07-04 边框宽度变更重绘 [L0]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 将边框宽度参数从0调整为6 — 验证点：shouldRepaint 返回 true
  2. 观察边框显示 — 验证点：边框宽度更新为新值
- **预期结果**：修改 borderWidth 属性触发重绘，边框宽度更新
- **后置条件**：将边框宽度复位为0
- **覆盖设备**：phone,tablet,2in1

### F-07-05 圆角半径变更重绘 [L0]
- **前置条件**：进入示例页面，关闭“圆形模式”切换为矩形模式
- **测试步骤**：
  1. 将统一圆角参数从0调整为32 — 验证点：shouldRepaint 返回 true
  2. 观察圆角显示 — 验证点：圆角半径更新为新值
- **预期结果**：修改 cornerRadius 属性触发重绘，圆角更新
- **后置条件**：将统一圆角参数复位为0
- **覆盖设备**：phone,tablet,2in1

### F-07-06 遮罩颜色变更重绘 [L0]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 将遮罩颜色从透明改为非透明 — 验证点：shouldRepaint 返回 true
  2. 观察遮罩显示 — 验证点：遮罩颜色更新
- **预期结果**：修改 maskColor 属性触发重绘，遮罩更新
- **后置条件**：将遮罩颜色恢复为透明
- **覆盖设备**：phone,tablet,2in1

## F-08 边界条件

### F-08-01 空图片渲染 [L2]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 将 image 属性设置为 null 并构建组件 — 验证点：组件渲染空区域且无异常
  2. 观察组件区域 — 验证点：无图片内容显示，组件正常存在
- **预期结果**：image 为 null 时组件正常渲染不崩溃
- **后置条件**：恢复图片显示
- **覆盖设备**：phone,tablet,2in1

### F-08-02 零边框宽度 [L2]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 设置边框宽度为0 — 验证点：无边框绘制，图片完整显示
  2. 观察组件边缘 — 验证点：无边框线条，图片边缘干净
- **预期结果**：borderWidth 为0时不绘制边框，图片完整显示
- **后置条件**：保持边框宽度为0
- **覆盖设备**：phone,tablet,2in1

### F-08-03 零尺寸渲染 [L2]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 将 width 和 height 设置为0并构建组件 — 验证点：组件零尺寸渲染不崩溃
  2. 观察页面状态 — 验证点：组件区域不显示内容但应用无异常
- **预期结果**：width 和 height 为0时组件正常渲染不崩溃
- **后置条件**：恢复组件默认尺寸
- **覆盖设备**：phone,tablet,2in1

### F-08-04 透明遮罩无效果 [L2]
- **前置条件**：进入示例页面，组件区域正常显示图片
- **测试步骤**：
  1. 将遮罩颜色设置为透明色 — 验证点：图片正常显示且无遮罩叠加
  2. 对比有遮罩与无遮罩的画面 — 验证点：透明遮罩不产生任何视觉变化
- **预期结果**：maskColor 为透明时不叠加遮罩，图片正常显示
- **后置条件**：保持遮罩颜色为透明
- **覆盖设备**：phone,tablet,2in1

### F-08-05 默认值验证 [L0]
- **前置条件**：进入示例页面，组件区域显示占位内容
- **测试步骤**：
  1. 使用默认参数创建组件并核对各项属性默认值 — 验证点：isCircle 为 false、borderWidth 为0、maskColor 为透明等
  2. 逐一核对全部构造参数的默认值 — 验证点：各参数默认值与 Android 完全一致
- **预期结果**：全部构造参数默认值与 Android 默认值一致
- **后置条件**：重置组件为默认状态
- **覆盖设备**：phone,tablet,2in1
