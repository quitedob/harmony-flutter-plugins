/*
 * TEMPLATE STUB — replaced by RNOH autolinking on hvigor build.
 * DO NOT modify it manually, your changes WILL be overwritten.
 *
 * prepare-only 后可能仍是占位内容；build hap 时 hvigor autolink 会按 entry/oh-package.json5
 * 的 HAR 依赖重写本文件。RNPackagesFactory.ets 须始终 spread createRNOHPackagesAutolinking(ctx)。
 */
import type { RNPackageContext, RNOHPackage } from '@rnoh/react-native-openharmony';

export function createRNOHPackages(ctx: RNPackageContext): RNOHPackage[] {
  return [];
}
