{
  "pluginName": "{{plugin_name}}",
  "pluginDescription": "{{plugin_description}}",
  "testScope": {
    "description": "{{test_scope_description}}",
    "priorityNote": "{{priority_note}}"
  },
  "modules": [
    {
      "moduleCode": "{{module_code}}",
      "moduleName": "{{module_name}}",
      "moduleDescription": "{{module_description}}",
      "priority": "{{priority}}",
      "testCases": [
        {
          "caseId": "{{case_id}}",
          "title": "{{case_title}}",
          "level": "{{case_level}}",
          "preconditions": "{{preconditions}}",
          "steps": "{{steps}}",
          "expected": "{{expected}}"
        }
      ]
    }
  ],
  "coverage": {
    "P0": {
      "count": {{p0_count}},
      "percentage": "{{p0_percentage}}%"
    },
    "P1": {
      "count": {{p1_count}},
      "percentage": "{{p1_percentage}}%"
    },
    "P2": {
      "count": {{p2_count}},
      "percentage": "{{p2_percentage}}%"
    },
    "total": {{total_count}}
  },
  "generatedAt": "{{iso8601_timestamp}}"
}
